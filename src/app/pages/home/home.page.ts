import { Component } from '@angular/core';
import { IonContent } from '@ionic/angular';

@Component({
  selector: 'app-home',
  imports: [IonContent],
  template: '<ion-content>HomePage</ion-content>',
})
export class HomePage {}
